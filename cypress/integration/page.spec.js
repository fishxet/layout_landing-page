'use strict';

// TODO: edit it
// eslint-disable-next-line no-undef
describe('Page', () => {
  // eslint-disable-next-line no-undef
  it('should be visitable', () => {
    // eslint-disable-next-line no-undef
    cy.visit('/');
  });
});
